.grid
  - result.each do |movie|
    .grid-item
      %p.title= "#{movie[:name]} (#{movie[:year]})"
      %div.poster
        = image_tag(movie[:cover]) if movie[:cover]
      %p.description= movie[:description]
      %p.genres= movie[:genres]
